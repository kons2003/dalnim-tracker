spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL # 메모리에만 존재하는 H2를 MySQL 모드로 흉내 내어 실행
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop # 테스트가 시작될 때 테이블을 만들고, 끝나면 싹 지움